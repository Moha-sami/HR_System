{
  "name": "hr-system-mock-api",
  "version": "1.0.0",
  "private": true,
  "description": "Glitch entry point: serves the HR System mock JSON API (json-server) from the nested JsonServer folder.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "npx json-server --watch src/Buy2.Frontend/Front/JsonServer/data.json --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
